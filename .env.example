# Authentication (required)
# Generate AUTH_SECRET with: npx auth secret
AUTH_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# Optional AI narration / image extraction. Keep server-side only.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5

# Future production persistence adapter
MONGODB_URI=
